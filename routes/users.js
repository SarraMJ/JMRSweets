/*
var express = require('express'); //exporte express dans notre appli
var router = express.Router(); // créée une instance d'un router express

// this part handles http post requests related to users 
router.post('/', function(req, res, next) {
//^ sets up a route ot listen for http posts requests, req = reuqest, res = response  
  res.send('respond with a resource');
    // respond with a ressource is to be replaced with what we want to send back to the client (eg saving data to a database or performing action related to users)
});

module.exports = router;
*/